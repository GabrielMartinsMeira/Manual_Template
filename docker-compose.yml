version: '3.8'

services:
  manual-template:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: manual-template
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
